import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 61,
    borderRadius: 8,
    backgroundColor: '#151FCB',
  },
  label: {
    textAlign: 'center',
    fontSize: 20,
    color: 'white',
    fontWeight: '600',
  },
  btnDisabled: {
    backgroundColor: '#cccccc',
  },
});

export default styles;
